def loadConfiguration() {
	def env = project.getProperty('env')
	def configFile = file('Config.groovy')
	def configObject = new ConfigSlurper(env).parse(configFile.toURL())

	ext {
		environment = env
		config = configObject
	}
}

task showProperties << {
	println "Location is ${config.location}"
}